# Per-agent pointer — Claude Code

> **Read [`../../AGENTS.md`](../../AGENTS.md) first.** That file is the workspace source of truth.
>
> **Also read [`../../CLAUDE.md`](../../CLAUDE.md)** for Claude-Code-specific umbrella rules in this workspace.

This stub exists so Claude Code is treated symmetrically with the other agents in `.agents/`. Claude Code's native config lives at `~/.claude/CLAUDE.md` (global, not touched by workspace setup).
