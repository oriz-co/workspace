# Per-agent pointer — OpenCode

> **Read [`../../AGENTS.md`](../../AGENTS.md) first.** That file is the workspace source of truth.

OpenCode auto-discovers `AGENTS.md` at workspace root, so this stub is informational only. Place OpenCode-specific overrides below this line if any are ever needed.

---

(none)
