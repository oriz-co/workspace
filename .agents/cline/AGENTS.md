# Per-agent pointer — Cline

> **Read [`../../AGENTS.md`](../../AGENTS.md) first.** That file is the workspace source of truth.

Cline natively supports `AGENTS.md` at workspace root, so this stub is informational only. For Cline-specific overrides (e.g. tool approvals, auto-approve patterns), put them below.

---

(none)
