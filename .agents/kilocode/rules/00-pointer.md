# Per-agent pointer — Kilo Code

> **Read [`../../../AGENTS.md`](../../../AGENTS.md) first.** That file is the workspace source of truth.

Kilo Code reads every `.md` file in `.kilocode/rules/` and merges them. This stub keeps Kilo Code aligned with the other agents by pointing at AGENTS.md.

For Kilo Code to pick this up, also symlink or copy this dir to `<repo>/.kilocode/rules/` via the installer.

---

Kilo-Code-specific overrides:

(none)
